import pygame

pygame.init()

screen_width = 1000
screen_height = 1000

window = pygame.display.set_mode((screen_width,screen_height))
pygame.display.set_caption('RPS Fight!')

vel_x = 0
vel_y = 0

while running
    for even in pygame.event.get():
        if event.type == pygame.QUIT:
            running = False
